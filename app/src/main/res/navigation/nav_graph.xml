<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.yuruneji.cameratraining2.presentation.home.HomeFragment"
        android:label="@string/title_home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_home_to_camera"
            app:destination="@+id/navigation_camera" />
        <action
            android:id="@+id/action_home_to_setting"
            app:destination="@+id/navigation_setting" />
        <action
            android:id="@+id/action_home_to_log_view"
            app:destination="@+id/navigation_log_view" />
    </fragment>

    <fragment
        android:id="@+id/navigation_camera"
        android:name="com.yuruneji.cameratraining2.presentation.camera.CameraFragment"
        android:label="@string/title_camera"
        tools:layout="@layout/fragment_camera">
        <action
            android:id="@+id/action_camera_to_home"
            app:destination="@+id/navigation_home" />
    </fragment>

    <fragment
        android:id="@+id/navigation_setting"
        android:name="com.yuruneji.cameratraining2.presentation.setting.SettingFragment"
        android:label="@string/title_setting"
        tools:layout="@layout/fragment_setting">
        <action
            android:id="@+id/action_setting_to_home"
            app:destination="@+id/navigation_home" />
    </fragment>

    <fragment
        android:id="@+id/navigation_log_view"
        android:name="com.yuruneji.cameratraining2.presentation.logview.LogViewFragment"
        android:label="@string/title_log_view"
        tools:layout="@layout/fragment_log_view">
        <action
            android:id="@+id/action_log_view_to_home"
            app:destination="@+id/navigation_home" />
    </fragment>

</navigation>
